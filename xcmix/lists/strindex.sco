/**
 * @brief Returns the indices at which string element x appears in list l
 * 
 * @param l list to search
 * @param x float element
 * @return list 
 */
list strindex(list l, string x) {
    return _getindex(l, {x})
}