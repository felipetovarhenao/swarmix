/**
 * @brief Returns the indices at which float element x appears in list l
 * 
 * @param l list to search
 * @param x float element
 * @return list 
 */
list findex(list l, float x) {
    return _getindex(l, {x})
}